<%= form_for(@booking) do |f| %>
  <% if @booking.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@booking.errors.count, "error") %> prohibited this booking from being saved:</h2>

      <ul>
      <% @booking.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :booking_time %><br>
    <%= f.date_select :booking_time %>
  </div>
  <div class="field">
    <%= f.label :booking_passenger_ids %><br>
    <%= f.text_field :booking_passenger_ids %>
  </div>
  <div class="field">
    <%= f.label :booking_tour_id %><br>
    <%= f.number_field :booking_tour_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
